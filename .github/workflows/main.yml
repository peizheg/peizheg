name: Metrics
on:
  # Schedule daily updates
  schedule: [{cron: "0 0 * * *"}]
  # (optional) Run workflow manually
  workflow_dispatch:
  # (optional) Run workflow when pushing on master/main
  push: {branches: ["master", "main"]}
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    environment: 
      name: production
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}

          # Options
          user: peizheg
          template: classic
          base: header, activity, community, repositories, metadata
          base_hireable: yes
          config_timezone: America/Toronto
          plugin_isocalendar: yes
          plugin_isocalendar_duration: full-year
          plugin_languages: yes
          plugin_languages_skipped: peizheg/phonebook
          commits_authoring: Peizhe Guan, peizheg, Crazydodo123, peizheg@gmail.com
          plugin_languages_indepth: yes
          plugin_languages_details: percentage, lines
          plugin_languages_aliases: TSX:TypeScript
          plugin_languages_indepth_custom: redacted24/poker, mchacksteam25/mchacks25, sivabalansm/reddit_depression_analysis, LiveGuard-Streaming/MediaFlux-API, JStephenHuang/budget-buddy
          plugin_topics: yes
          plugin_topics_mode: icons
          plugin_languages_ignored: "jupyter notebook, roff, html, css, Kotlin, Objective-C++"
